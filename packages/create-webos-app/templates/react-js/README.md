# __DISPLAY_NAME__

由 `create-webos-app` 生成 · **React · JavaScript**（无 MUI / 无 TS）

> ⚠ 第一次启动前必读 [`PREREQUISITES.md`](PREREQUISITES.md)：`@webos/host-sdk` 不在 npm registry 上，需要先用 `npm link` 接到本地 webos 仓库。

```bash
npm link @webos/host-sdk     # 一次性，按 PREREQUISITES 配好后
npm install
npm run dev
```

如需 MUI 主题联动 → 用 `react-mui-js` 模板重建。
